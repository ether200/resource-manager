import { SpinnerWrapper, LoadingSpinner } from './Spinner.styles';

const Spinner = () => {
  return (
    <SpinnerWrapper>
      <LoadingSpinner />
    </SpinnerWrapper>
  );
};

export default Spinner;
